apiVersion: 1

providers:
    - name: 'VeriDraw'
      orgId: 1
      folder: 'VeriDraw'
      type: file
      disableDeletion: false
      updateIntervalSeconds: 10
      allowUiUpdates: true
      options:
          path: /etc/grafana/provisioning/dashboards